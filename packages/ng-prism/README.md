# ng-prism

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ng-prism` to build the library.

## Running unit tests

Run `nx test ng-prism` to execute the unit tests via [Jest](https://jestjs.io).
