{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ng-prism Build Builder",
  "description": "Build the ng-prism styleguide for production",
  "type": "object",
  "properties": {
    "entryPoint": {
      "type": "string",
      "description": "Path to the library's public-api.ts (entry point for scanning)"
    },
    "prismProject": {
      "type": "string",
      "description": "Angular project name for the Prism styleguide app"
    },
    "libraryProject": {
      "type": "string",
      "description": "Library project name (used to resolve source root)"
    },
    "outputPath": {
      "type": "string",
      "description": "Output directory for the production build"
    },
    "libraryImportPath": {
      "type": "string",
      "description": "NPM package name used for imports in the generated manifest (defaults to libraryProject)"
    },
    "configFile": {
      "type": "string",
      "default": "ng-prism.config.ts",
      "description": "Path to the ng-prism config file"
    },
    "cacheDir": {
      "type": "string",
      "description": "Override directory for the generated prism-manifest.ts. Default: node_modules/.cache/ng-prism/<prismProject>/. Relative paths resolve against workspaceRoot. Useful for CI sandboxes with read-only node_modules."
    }
  },
  "required": ["entryPoint", "prismProject"],
  "additionalProperties": false
}
