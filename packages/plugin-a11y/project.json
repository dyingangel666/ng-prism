{
  "name": "plugin-a11y",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["ng-prism:build"],
      "options": {
        "command": "node_modules/.bin/ngc --project packages/plugin-a11y/tsconfig.lib.json"
      },
      "inputs": ["production", "^production"],
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
