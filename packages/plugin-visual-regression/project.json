{
  "name": "plugin-visual-regression",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["ng-prism:build"],
      "options": {
        "command": "node_modules/.bin/ngc --project packages/plugin-visual-regression/tsconfig.lib.json"
      },
      "inputs": ["production", "^production"],
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
